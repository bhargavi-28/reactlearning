export function ColorBoxs({ newc }) {
  const styles = {
    backgroundColor: newc,
    height: "30px",
    width: "200px",
    margin: "20px"
  };
  return (
    <div style={styles}>



    </div>
  );
}
